## What changed

Describe the behavior and why it is useful.

## Validation

- [ ] `dotnet build .\MomentsToDiscord.csproj -c Release` succeeds without warnings
- [ ] Relevant first-run, watcher, upload, retry, or move behavior was tested
- [ ] Documentation was updated for user-facing changes
- [ ] No webhook URLs, clips, local settings, FFmpeg binaries, or build artifacts were committed
