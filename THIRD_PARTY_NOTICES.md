# Third-party notices

Clips to Discord release packages may include an `ffmpeg.exe` binary to compress clips that Discord rejects for size.

FFmpeg is a separate project and is not covered by this repository's MIT License. Its licensing depends on the features enabled in the particular build. Distributors must include the license shipped with that build and follow its redistribution requirements. FFmpeg source and licensing information are available from the FFmpeg project and the binary build provider.

This repository does not commit or vendor an FFmpeg binary.
